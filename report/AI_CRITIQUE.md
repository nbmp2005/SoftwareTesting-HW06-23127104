# AI Critique (Mandatory, 200–300 words)

> Final word count: `[200–300]`. Replace this scaffold with your own critique based on actual interactions. Do not claim an AI mistake that did not occur.

During this assignment, I used `[AI TOOL]` to `[actual tasks]`. The most important incomplete or incorrect output occurred when `[specific interaction and test-case ID]`. The AI proposed `[what it proposed]`, but the requirement states `[exact rule]`; therefore I classified the case as `[INVALID/INCOMPLETE]` and corrected it by `[correction]`.

The failure likely happened because `[prompt omitted context / requirement was implicit / state combinations were complex / model relied on a common API convention]`. A second limitation was `[specific security/state/schema omission]`. I detected it by `[human review method: traceability matrix, transition table, boundary analysis, execution]`, then added `[human TC IDs]`. Execution showed `[real observation]`; this result `[confirmed/rejected]` the AI's assumption.

The main principle I learned is `[your lesson]`. In particular, AI is useful for expanding candidate partitions and structuring test ideas, but correctness still depends on a human-defined oracle and reproducible evidence. My collaboration improved when I split the task into contract extraction, coverage modelling, case generation and audit instead of requesting a complete suite in one prompt. For future work, I would `[specific improvement]`, while continuing to preserve raw outputs, label every case, and verify all claims against the specification and test execution.

<!-- Count only the final critique paragraph(s), not this heading/note. Suggested command: use your editor word count or a verified script. -->
